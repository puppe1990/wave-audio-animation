{
  "name": "wave-audio-animation-workspace",
  "private": true,
  "scripts": {
    "dev": "(cd backend && source venv/bin/activate && uvicorn app.main:app --reload) & (cd frontend && npm run dev) && wait",
    "dev:back": "cd backend && source venv/bin/activate && uvicorn app.main:app --reload",
    "dev:front": "cd frontend && npm run dev"
  }
}
